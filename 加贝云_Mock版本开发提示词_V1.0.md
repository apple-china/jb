# 加贝云 Mock 版本开发提示词

> 2026-09-06：本文是旧 Mock 开发任务的历史提示词，不应原样作为新版开发指令。后续业务规则以 [V1.0 确认需求基线](docs/加贝云_V1.0_确认需求基线.md) 为准；原 V1.1 门禁、签到和迟到群提醒已进入 V1.0，旧文中的排除项不再覆盖新基线。

将以下内容作为 `D:\GPT\JiaBei` 项目中的首条开发指令发送给 Codex。

---

你是本项目的资深全栈开发工程师、测试工程师和技术负责人。请在当前本地项目中完成“加贝云·化妆预约”产品 V1.0 的可运行 Mock 版本，不要只生成设计、脚手架、静态页面或伪代码。

## 一、项目位置与输入文件

项目根目录：

```text
D:\GPT\JiaBei
```

目录约定：

```text
D:\GPT\JiaBei
├─ docs\
│  ├─ 加贝云_化妆预约项目说明书_V1.0.docx
│  ├─ 加贝云_技术设计说明书_V1.0.md
│  ├─ 加贝云_第三方接口契约_V1.0.md
│  ├─ 加贝云_需求覆盖矩阵_V1.0.md
│  └─ 加贝云_验收清单与测试用例_V1.0.xlsx
├─ assets\
└─ src\
```

开始开发前必须完整阅读上述五份文档，并检查项目根目录或上级目录中的 `AGENTS.md`、README、现有代码和配置。不要只根据本提示词猜测业务细节。

`D:\GPT\JiaBei\assets` 是项目参考素材目录，不是完整设计稿或现成 UI 资源库。目前仅提供 Logo 和头像。开发前先盘点其中的文件、格式、尺寸和透明背景情况，并在界面中优先复用合适素材；不要假设目录中还包含图标库、页面效果图、字体、颜色规范或完整设计系统。缺失的通用界面图标优先使用所选前端组件库的统一图标，其他缺失视觉资源使用简洁的本地占位内容，并在 README 的“待替换素材”清单中记录，禁止临时联网抓取来源不明或存在版权风险的图片。

文档优先级如下：

1. `加贝云_化妆预约项目说明书_V1.0.docx`：业务规则最高优先级。
2. `加贝云_技术设计说明书_V1.0.md`：架构、数据库、状态机、内部 API、权限和测试实现依据。
3. `加贝云_第三方接口契约_V1.0.md`：第三方接口、字段和 Mock 行为依据。
4. `加贝云_验收清单与测试用例_V1.0.xlsx`：验收范围和测试编号依据。
5. `加贝云_需求覆盖矩阵_V1.0.md`：需求追踪依据。

如文档之间存在冲突，以项目说明书为准，不得擅自改变业务规则。将冲突记录到 `docs/开发问题与假设.md`；不影响核心开发的事项采用最保守、可配置且可撤销的实现继续推进。只有会导致数据模型或核心流程完全不同的问题才停止并向我确认。

## 二、本次目标

完成一个能在本地启动、操作、测试和验收的产品 V1.0 Mock 版本，包括：

- 主播端移动优先页面。
- 管理端移动端和 PC 响应式页面。
- 后端内部 API、鉴权、业务规则和审计。
- PostgreSQL 数据库结构、Flyway 迁移和足够的初始化数据。
- 钉钉免登与互动卡片的本地 Mock 适配器。
- 单元测试、集成测试、并发测试和前端端到端测试。
- 本地启动说明、测试账号、Mock 使用说明和验收结果。

本次只实现“产品 V1.0”。文档中标注为产品 V1.1、后续版本或明确排除的内容不得实现为半成品页面、状态、按钮或可调用 API。

明确不实现：

- 魔点门禁回调、到司状态和迟到提醒。
- 主播修改预约。
- 主播或管理员调换、确认、审批状态机。
- 主管理员/子管理员分级。
- Excel 导出、批量操作、复杂报表和完整时间线。
- 卡片内直接预约或取消。
- 管理员操作后的个人通知。
- 多组织、多群和多门禁。

## 三、技术实现要求

优先采用技术设计说明书中的参考栈：

- 前端：Vue 3、TypeScript、Vite。
- 后端：Java 21、Spring Boot 3。
- 数据库：PostgreSQL 16。
- 数据访问：根据技术设计和现有工程选择 MyBatis 或 Spring Data JPA，只保留一种。
- 数据迁移：Flyway。
- 接口：REST/JSON，并维护 OpenAPI 描述。
- 测试：JUnit 5、Testcontainers、前端单元测试和 Playwright。

若本机缺少某项运行条件，先检查已有工具和项目配置，再选择与文档一致的最小替代方案。不得用 H2、内存数组或仅前端假数据代替 PostgreSQL 的唯一约束、排斥约束和事务测试。

项目结构应清楚区分：

```text
src/
├─ frontend/
├─ backend/
├─ db/                  # 如迁移归后端管理，可放入 backend/resources/db/migration
└─ tests/               # 跨端或验收测试；单元测试可放各子工程
```

可以根据构建工具调整具体层级，但不得把前后端、迁移和测试混在一个难以维护的目录中。

## 四、V1.0 必须实现的业务闭环

### 4.1 身份与权限

- 管理员进入管理端。
- 有效主播进入主播端。
- 其他员工进入无权限页，且不能读取预约数据。
- 管理员角色优先于主播角色。
- 每个后端接口重新校验当前用户、角色、资源归属、状态和权限。
- 管理员停用后，原会话的管理权限立即失效。
- 不能相信前端传入的角色、userId、姓名或管理员标志。

### 4.2 主播端

- 使用单页结构，包含品牌区、今天/明天切换、我的预约、快速预约、当日预约情况和规则说明。
- 预约和取消使用底部弹层，规则说明使用全屏弹层。
- 今天没有合法可用时段时默认选中明天。
- 预约表单包含只读主播、老师、时间和团队。
- 老师、时间、团队上次选择仍合法时自动选中；失效、冲突或不满足提前量时清空并解释原因。
- 时间使用 10 分钟时间块，不允许任意输入。
- 创建和取消成功后刷新本人预约、当日安排，并写入卡片刷新任务。
- 主播只能创建和取消，不能修改或调换。

### 4.3 预约硬约束

- 统一使用服务器 `Asia/Shanghai` 时间。
- 服务时长默认 20 分钟，最小提前预约时间默认 30 分钟。
- 同一主播同一预约日期最多一条非作废记录。
- `ACTIVE` 和 `CANCELLED` 均占用主播该日期资格；`VOIDED` 才释放。
- 同一老师的 `ACTIVE` 预约时间段不可重叠，相邻半开区间可以共存。
- 老师必须启用、未休息，预约服务结束时间不得晚于下班时间。
- 团队必须启用。
- 普通预约、管理员代预约和管理员修改必须共用同一套硬约束，管理员不能强制制造冲突。
- 预约状态只允许 `ACTIVE`、`CANCELLED`、`VOIDED`。

### 4.4 主播取消

- 只能取消本人 `ACTIVE` 预约。
- 当前时间恰好等于预约开始前 30 分钟时允许取消；晚于截止点不允许。
- 同一主播按点击取消按钮时的 `Asia/Shanghai` 自然日，每天最多成功取消一次。
- 取消今天或明天的预约都会消耗点击当天的取消额度。
- 同一幂等键重复请求只执行一次、只记一次。
- 管理员取消不消耗主播取消额度。
- 取消后释放老师时段，但预约日期资格仍被占用，只有管理员作废后才允许重新预约。

### 4.5 管理端

- 一级页面为“预约、 新建、设置”。
- 预约列表支持今天、明天、自定义日期，以及主播、老师、预约状态筛选，默认按预约时间升序。
- 详情显示预约 ID、钉钉 userId、提交时间、操作人、原因和修改前后值。
- 管理员可代预约、修改老师/时间/团队、取消和作废。
- 管理员不能修改预约日期。
- 距预约开始时间小于或等于 5 分钟时，管理员不能修改或取消。
- 修改、取消、作废和管理员名单变更必须填写原因，并记录完整审计信息。
- 系统写开关关闭时，主播只读；管理员仍可查看和执行管理操作。
- 老师管理包含名称、头像、上下班时间、休息和启停。
- 团队管理包含名称、Logo 和启停。
- 老师休息/停用或团队停用时，如存在未来 `ACTIVE` 预约，首次请求返回预约数量且不修改；用户二次确认后锁定资源、重新计数并生效，不自动取消已有预约。
- V1.0 所有管理员同级，可配置多人；任何管理员名单变更提交后都必须至少保留一名启用管理员，并能抵抗并发互相停用。

### 4.6 每日群互动卡片 Mock

- 每天 09:00 按 `Asia/Shanghai` 生成一张今日安排卡片。
- 公开区按预约开始时间升序展示当天全部 `ACTIVE` 预约的时间、主播、老师和团队。
- 每个启用主播通过 `privateData` 获得自己的“我的预约”区域。
- 纯管理员不生成私有区；同时具有主播身份的管理员按主播规则生成。
- 卡片只有一个“进入预约”按钮。
- 同一群、同一日期只有一个当前 `outTrackId`。
- 预约变化更新原卡，不重复新建消息。
- 连续更新必须使用内容版本，旧任务不能覆盖新内容。
- 只有模拟“卡片删除、过期或不可恢复”时才能补发替代卡片。
- 卡片失败不能回滚预约事务。

## 五、第三方接口 Mock 要求

默认配置必须完全离线，不访问真实钉钉或魔点，不需要公网域名，也不需要真实 Secret。

为第三方能力建立接口适配层，业务服务不得直接调用第三方 HTTP 客户端：

1. 钉钉客户端免登适配器：模拟 `dd.requestAuthCode`。
2. 钉钉 Token 适配器：模拟 DT-02，支持缓存、临近过期、并发刷新和失效错误。
3. 钉钉用户信息适配器：模拟 DT-03，用固定 authCode 映射测试身份。
4. 钉钉卡片适配器：模拟 DT-04 创建投放和 DT-05 更新。

Mock 卡片适配器至少保存：

- `outTrackId`。
- 群和业务日期。
- 公共 `cardData`。
- 按 userId 隔离的 `privateData`。
- 内容版本、投放状态、更新时间和调用日志。

提供仅限 local/test 的 Mock 查看方式，能够查看“群里当前卡片”“指定用户看到的私有内容”和调用历史。可以采用本地调试页或测试专用 API，但生产配置下必须不存在。

提供可控故障注入：

- Token 过期。
- 网络超时。
- HTTP 5xx。
- 卡片更新结果不确定。
- 卡片已删除或过期。
- 多个刷新任务乱序执行。

所有真实集成实现必须由配置显式启用。本次不要求连接真实接口，但接口类型、请求模型和适配器边界应与第三方接口契约一致。不得编造契约中未确认的第三方字段。

为后续安全测试环境联调预留以下环境变量，但当前 Mock 阶段只创建变量名为空的 `.env.example`，不得要求我把真实值粘贴到对话、提示词、README、测试报告或源代码中：

```dotenv
DINGTALK_CORP_ID=
DINGTALK_CLIENT_ID=
DINGTALK_CLIENT_SECRET=
MOREDIAN_ORG_ID=
MOREDIAN_ORG_AUTH_KEY=
```

- `DINGTALK_CLIENT_ID`：钉钉 Client ID。企业内部应用对应原 AppKey；本项目不使用服务商模式的 SuiteKey。
- `DINGTALK_CLIENT_SECRET`：钉钉 Client Secret。企业内部应用对应原 AppSecret；本项目不使用 SuiteSecret。
- `DINGTALK_CORP_ID`：钉钉企业 CorpId。
- `MOREDIAN_ORG_ID`：魔点 OrgID；第三方接口字段名为 `orgId`，仅为产品 V1.1 预留。
- `MOREDIAN_ORG_AUTH_KEY`：魔点 OrgAuthKey，高度敏感，仅为产品 V1.1 预留。

同时生成 `.env.integration.local` 的加载约定并将该文件加入 `.gitignore`，但不要创建包含真实值的文件。Mock profile 不读取真实凭证；integration profile 才允许连接测试应用。任何命令、日志、异常、HTTP 调试输出、截图和测试结果都不得显示 Client Secret、OrgAuthKey、AccessToken、免登码或完整签名。

生产 profile 必须满足：

- `/auth/mock-login` 路由不存在或返回 404。
- 前端不存在 Mock 身份切换入口。
- 未提供真实配置时不会意外调用外网。
- 日志不输出免登码、AccessToken、AppSecret 或完整签名。

## 六、数据库与可靠性要求

必须实现技术设计说明书中的数据库结构和约束，至少包括：

- 用户/角色、主播偏好、老师、团队、预约、操作审计。
- API 幂等记录。
- 主播取消日账本。
- 系统开关。
- 每日卡片索引。
- 数据库发件箱任务。

必须在 PostgreSQL 层兜底：

- 主播同一预约日期最多一条非作废预约。
- 老师 `ACTIVE` 预约区间不可重叠。
- 主播同一点击自然日最多一条成功取消账本。
- 同群同业务日期只有一张当前卡片。
- 稳定业务幂等键唯一。

管理员新增、启用和停用必须使用技术设计中的统一事务级锁策略；并发结果不得出现零名启用管理员。

预约创建事务至少同时包含预约、名称快照、审计、偏好、幂等结果和卡片刷新任务。主播取消事务至少同时包含状态变更、取消日账本、审计、幂等结果和卡片刷新任务。任一步失败必须整体回滚。

第三方发送通过数据库发件箱异步执行，使用有限指数退避、稳定业务键、任务租约和失败状态；禁止无限重试。

## 七、Mock 数据

提供足够但不过量的固定测试数据，至少包括：

- 两名启用管理员：`admin01`、`admin02`。
- 一名停用管理员：`admin_disabled`。
- 四名启用主播：`streamer01`～`streamer04`。
- 一名无权限员工：`employee01`。
- 至少两名正常老师、一名休息老师、一名停用老师。
- 至少三个启用团队和一个停用团队。
- 今天、明天的正常预约、相邻时段预约、已取消预约、已作废预约和有变更历史的预约。
- 可以验证上次选择、同日资格、老师冲突、取消限次、管理员截止和卡片更新的数据。

Mock 数据应可重复初始化。测试账号、authCode 映射和数据含义写入 README，不使用真实个人信息。

## 八、界面要求

- `assets` 当前仅包含参考用 Logo 和头像。先检查其适用场景与显示质量，再复制或引用到规范的前端静态资源目录；不得修改或覆盖原始参考素材。
- 不因缺少完整设计稿而阻塞 Mock 开发，应依据项目说明书完成简洁、统一、可验收的界面；不得自行扩展新的品牌视觉规范或增加与需求无关的装饰。
- 缺失资源使用组件库图标或清晰的本地占位资源，并在 README 中记录待替换文件、使用位置和建议规格。
- 主播端以 360×800 为主要验收尺寸，不允许横向滚动。
- 管理端同时适配手机和常见 PC 宽度。
- 移动端列表只展示必要字段，详情使用底部抽屉或全屏页面。
- PC 管理端可以使用表格和侧边详情。
- 时间必须使用可点击的时间块，已占用时间直接禁用。
- 提交按钮在弹层底部固定，提交中禁用。
- 错误提示使用项目说明书中的正式文案，不把数据库异常或技术错误直接显示给用户。
- 页面应精致、克制、易读，不为了视觉效果增加业务复杂度。

## 九、测试要求

以 Excel 中的验收项和测试用例编号为测试基线。优先完成所有产品 V1.0 的 P0，再完成 V1.0 的 P1。

至少执行并保留结果：

- 后端领域规则单元测试。
- PostgreSQL/Testcontainers 集成测试。
- 幂等、同日唯一、老师重叠、取消日账本、管理员名单的并发测试。
- 预约和发件箱事务回滚测试。
- 权限、IDOR、参数伪造、CSRF、Origin、会话失效和生产禁用 Mock 测试。
- Token 缓存、卡片创建/更新、乱序、超时、重试和替代卡片 Mock 契约测试。
- 前端组件测试。
- Playwright 主播端、管理端和无权限端核心流程。
- 360×800、常见 PC 宽度的页面检查。

不得通过删除断言、跳过失败测试、降低数据库约束或把测试改成永远成功来获得绿色结果。无法执行的测试必须注明真实原因和复现命令。

建议补充覆盖矩阵中列出的缺口用例，尤其是：

- 默认日期选择。
- 刷新复用会话。
- 管理员日期不可变和原因必填。
- 管理员并发修改版本冲突。
- 创建/取消事务中途失败回滚。
- 空卡片、状态过滤、私有区角色组合和卡片不可恢复降级。
- 登录限流、安全响应头和图片上传边界。

## 十、开发执行方式

这是一个连续完成的开发任务，但不要无验证地一次性堆出全部代码。请按可运行的纵向阶段实施，并在每个阶段完成编译和测试后继续：

1. 检查文档、环境、现有文件和依赖，输出简短实施计划。
2. 建立工程、开发配置、PostgreSQL/Flyway 和固定 Mock 数据。
3. 完成身份、会话、权限和 Mock 登录。
4. 完成预约领域、数据库硬约束、幂等、事务和审计。
5. 完成主播端闭环。
6. 完成管理端闭环和基础设置。
7. 完成每日卡片投影、Mock 适配器、发件箱和故障注入。
8. 完成自动化测试、端到端测试和视觉检查。
9. 更新需求覆盖矩阵中的真实代码路径、实现状态和测试对应关系。
10. 完成 README、启动脚本、环境变量样例和最终交付报告。

在不破坏用户文件的前提下自主推进。不要使用破坏性 Git 或文件命令，不要覆盖用户已有且与本任务无关的修改。新增依赖前优先使用成熟、必要且与现有栈兼容的库，避免引入微服务、MQ、Redis 和复杂基础设施。

## 十一、必须产出的文件

至少产出并维护：

- 完整前端与后端源代码。
- PostgreSQL/Flyway 迁移。
- Mock 初始化数据。
- `.env.example`，不得包含真实密钥。
- 本地启动方式；如适用，提供 `docker-compose.yml`。
- OpenAPI 描述或可导出的接口文档。
- 自动化测试代码和测试结果。
- `README.md`。
- `docs/开发问题与假设.md`。
- 更新后的 `docs/加贝云_需求覆盖矩阵_V1.0.md`。
- `docs/Mock版本验收报告_V1.0.md`。

README 必须写明：

- 所需软件版本。
- 一条清晰的首次启动流程。
- 前后端地址和健康检查地址。
- 数据库初始化方式。
- Mock 测试账号和 authCode。
- 如何切换角色。
- 如何查看模拟群卡片和 privateData。
- 如何触发第三方故障模拟。
- 如何执行全部测试。
- 如何重置测试数据。

## 十二、完成标准

只有满足以下条件才可以宣布完成：

- 前后端能够在本地实际启动。
- PostgreSQL 迁移和初始化数据成功。
- 管理员、主播和无权限员工三种身份均可验证。
- 主播预约、取消和管理员纠错形成完整可操作闭环。
- 数据库硬约束、幂等、事务和并发测试通过。
- 每日卡片 Mock 能创建、更新、展示公共数据和不同用户的 privateData。
- 生产配置不存在 Mock 登录入口。
- 所有产品 V1.0 P0 用例通过；P1 失败项有明确记录。
- 前端和后端构建成功，没有未解释的跳过测试。
- 需求覆盖矩阵已回填真实代码位置。
- 最终报告明确区分“Mock 已验证”和“仍需真实钉钉联调”，不得声称已完成真实第三方验收。

最终回复请先给出完成结论，再列出：

1. 已实现的模块。
2. 关键技术决策。
3. 启动和测试命令。
4. Mock 账号与操作入口。
5. 自动化测试结果。
6. 未完成项或真实联调项。
7. 重要文件路径。

现在开始检查项目并持续实施，不要在只创建脚手架或静态页面后停止。
